# 模型设计方案.md

> **项目名称：** HiDFilter  
> **任务：** 多步交通时序预测  
> **主要目标：** 在 BasicTS 实验基础设施上，从空白工作区实现一个可稳定训练、运行效率可接受、实验协议可复现的 HiDFilter 模型，并优先在 PEMS08 上完成正确性、性能和效果闭环，再扩展到 PEMS03 / PEMS04 / PEMS07 / METR-LA / PEMS-BAY。  
> **首要运行环境：** 华为 Ascend NPU  
> **第一阶段精度：** FP32  
> **历史长度：** 12  
> **预测长度：** 12  
> **本文定位：** 只规定模型实现、实验协议、训练流程、性能要求、测试要求和实验执行顺序。本文不讨论论文写作、相关工作或贡献表述。  
> **唯一原则：** BasicTS 负责实验脚手架；HiDFilter 模型本体独立实现；STID 只用于 Phase 0 sanity baseline，不作为 HiDFilter 的模型母体。

---

# 0. Codex 执行规则

## 0.1 第一轮禁止写代码

Codex 完整读取本文档后，第一轮只能提交：

> **《HiDFilter 实现理解报告》**

不得创建、修改或生成任何代码文件。

报告必须包含：

1. **BasicTS 使用方式**
   - BasicTS 哪些基础设施直接复用；
   - 哪些地方因为 Ascend 需要薄适配；
   - 哪些部分绝对不重新实现。

2. **STID Phase 0 计划**
   - 如何在相同 BasicTS + Ascend 环境上跑 STID；
   - 需要记录哪些速度和指标；
   - Phase 0 的通过条件。

3. **HiDFilter 模块划分**
   - Context Encoder；
   - Lag Content Encoder；
   - Family Evidence；
   - Family Router；
   - Fine Sensor-Lag Filter；
   - Top-p；
   - Decoder；
   - Candidate Builder；
   - Diagnostics。

4. **精确 Tensor Flow**
   - 每个主要 tensor 的 shape；
   - lag 索引；
   - candidate flatten 顺序；
   - global K/V 与 gather 顺序；
   - Router / Fine / Fusion / Decoder 顺序。

5. **性能关键点**
   - 哪些大 tensor 明确禁止；
   - 哪些 metadata 必须预计算；
   - 如何避免 NPU→CPU 高频同步；
   - diagnostics 如何在 device 上先 reduce。

6. **实现 Phase 划分**
   - Phase 0 到 Phase 8；
   - 每个 Phase 的 correctness gate；
   - 每个 Phase 的 performance gate。

7. **外部信息缺口**
   - 如果真实数据 artifact 的 adjacency 语义、路径等尚未确认，明确列出；
   - 不允许自行猜测。

输出报告后停止，等待批准。

---

## 0.2 不允许一次性写完整项目

HiDFilter 必须按增量 Phase 实现。

每个 Phase 完成后：

```text
tests
-> correctness gate
-> performance profile
-> 审查
-> 才能进入下一 Phase
```

任何 Phase 如果出现：

```text
明显数值错误
NaN / Inf
loss 不下降
NPU utilization 异常低
step time 无法解释地 >2x 增长
显存突然异常增长
```

则停止继续叠加模块，先定位当前 Phase。

---

## 0.3 不允许自行增加模型复杂度

未经明确批准禁止加入：

- Transformer；
- Multi-head Attention；
- Mamba；
- GCN stack；
- Graph Transformer；
- dense adaptive adjacency；
- ODE / DDE；
- FFT delay；
- wavelet；
- Granger；
- contrastive loss；
- diffusion；
- forecasting MoE experts；
- adversarial training；
- 多预测头。

第一目标是把当前设计干净、正确、高效地实现出来。

---

# 1. 实验基础设施

## 1.1 BasicTS 是唯一实验脚手架

实验工程以 BasicTS v1.1.0 的交通预测协议为基准。

优先复用 BasicTS 已有能力：

```text
dataset / split
scaler
mask semantics
metrics
DataLoader conventions
training loop skeleton
validation loop
early stopping
checkpoint
logging
baseline ecosystem
```

如果 BasicTS 原 Runner 存在 CUDA-specific 逻辑，不需要强行保留完整 Runner。

允许方案：

```text
BasicTS data + protocol + metrics
+
薄 NPU-native runtime adapter
```

但不允许重新设计一套与 BasicTS 不同的数据和评价协议。

---

## 1.2 STID 的角色

STID 不是 HiDFilter 的脚手架。

STID 只用于：

> **Phase 0 系统健康检查**

结构关系：

```text
BasicTS infrastructure
        |
        +--------------------+
        |                    |
        v                    v
      STID               HiDFilter
 sanity baseline       proposed model
```

HiDFilter：

- 不继承 STID；
- 不修改 STID model；
- 不复制 STID architecture；
- 只与 STID 共享实验基础设施。

---


## 1.3 当前 Ascend 云端硬件与并行实验原则

当前可用实验节点的 `npu-smi info` 快照为：

```text
npu-smi version: 24.1.0.3
NPU 型号: Ascend 910B4
NPU 数量: 8
逻辑设备: NPU 0 ~ NPU 7
每张卡 HBM: 32768 MB
Health: 8/8 OK
当前 AICore: 0%
当前无用户训练进程
空闲状态 HBM 占用约: 2821 ~ 2828 MB / 卡
温度约: 47 ~ 54 °C
```

因此当前机器的核心实验资源可以按：

\[
\boxed{8\times Ascend\ 910B4,\quad 32GB\ HBM/card}
\]

理解。

注意：

- `32768 MB` 是设备总 HBM，不等于训练进程可以无条件使用完整 32 GB；
- 当前即使无训练进程，每卡仍有约 2.8 GB HBM 被系统/runtime 占用；
- 正式实验不能把“32 GB 全部可用”写死；
- 每次新 session 开始都重新记录一次 `npu-smi info`，因为共享云端环境可能变化。

### 默认并行策略

本项目默认采用：

\[
\boxed{\text{one independent experiment process per NPU}}
\]

而不是一开始使用 8-card DDP。

也就是说：

```text
NPU 0 -> Run A
NPU 1 -> Run B
NPU 2 -> Run C
...
NPU 7 -> Run H
```

原因：

1. PEMS08 和 HiDFilter candidate-first 设计预计单卡即可容纳；
2. 多个 seed / ablation / decoder candidate 天然是独立任务；
3. 独立并行没有梯度同步开销；
4. 不改变单次实验 batch size 和 optimizer dynamics；
5. 调试失败时只影响对应 run；
6. 8 张卡最适合用于提高“实验吞吐量”，而不是盲目缩短单个小模型的 step。

因此第一优先级是：

> **8 卡并行跑 8 个独立实验，而不是把一个 PEMS08 run 拆成 8 卡训练。**

只有以下情况才讨论 DDP：

```text
单卡明确 OOM
或
单卡单 run 已被证明是主要 wall-clock 瓶颈
且
profile 证明通信开销可接受
```

引入 DDP 前必须重新确认：

- effective batch size；
- optimizer semantics；
- scheduler semantics；
- metric parity；
- checkpoint/resume；
- reproducibility。

默认正式 HiDFilter 实验：

```text
single process
single NPU
no DDP
```


# 2. 数据集

需要支持：

- PEMS03
- PEMS04
- PEMS07
- PEMS08
- METR-LA
- PEMS-BAY

首要开发：

```text
PEMS08
```

PEMS08 全部 gate 完成前，不同时展开 6 个数据集调试。

---

# 3. 数据输入协议

## 3.1 12 -> 12

统一：

```text
history length = 12
forecast horizon = 12
```

BasicTS 外部格式：

```text
history_data [B,L,N,F]
future_data  [B,H,N,F]
```

主模型输入只使用 traffic channel：

```text
history [B,12,N,1]
```

输出：

```text
prediction [B,12,N,1]
```

---

## 3.2 只使用预切分 train/val/test

预期文件：

```text
datasets/<DATASET>/
    train_data.npy
    val_data.npy
    test_data.npy
    train_timestamps.npy
    val_timestamps.npy
    test_timestamps.npy
    adj_mx.pkl
    meta.json
```

禁止：

```text
train + val + test
-> concatenate
-> split again
```

每个 split 独立滑窗。

若 split 长度为 \(T\)：

\[
X_s=D[s:s+12]
\]

\[
Y_s=D[s+12:s+24]
\]

\[
s=0,\ldots,T-24
\]

总窗口数：

\[
T-23
\]

窗口禁止跨 split。

冻结窗口数：

| Dataset | Train | Validation | Test |
|---|---:|---:|---:|
| METR-LA | 23967 | 3404 | 6832 |
| PEMS-BAY | 36458 | 5188 | 10401 |
| PEMS03 | 15701 | 5218 | 5220 |
| PEMS04 | 10172 | 3375 | 3376 |
| PEMS07 | 16911 | 5621 | 5623 |
| PEMS08 | 10690 | 3548 | 3549 |

PEMS08 必须做 exact count test。

---

# 4. 输入特征

主实验：

```text
traffic_only = true
F = 1
```

TOD / DOW / timestamps 不进入主模型。

timestamps 允许被 BasicTS loader 保留，但模型主实验必须满足：

```text
traffic 不变
timestamp 改变
=> eval prediction 不变
```

内部 learnable embedding：

- node embedding；
- lag embedding；
- horizon embedding；
- route-family embedding；
- fine-family embedding。

这些是模型参数，不是外部 observed feature。

---

# 5. 标准化与 Null Mask

## 5.1 Train-only global scalar Z-score

使用 raw train traffic：

\[
\mu=\operatorname{mean}(D_{train})
\]

\[
\sigma=\operatorname{std}(D_{train},ddof=0)
\]

规则：

- global scalar；
- 不是 per-node；
- 0 值参与 mean/std；
- \(\sigma=0\) 时使用 1；
- val/test 复用 train statistics。

实现必须用 golden test 对齐 BasicTS v1.1.0 语义。

---

## 5.2 Raw validity

在标准化前：

\[
M(x)
=
\neg \operatorname{isclose}
(x,0,\mathrm{atol}=5\times10^{-5})
\]

pipeline：

```text
raw traffic
-> raw validity
-> normalize
-> invalid normalized value = exact 0
```

input mask 不进入模型。

target validity 交给 loss / metrics。

数据包含 NaN / Inf：

```text
hard error
```

---

# 6. Metrics

训练：

```text
normalized masked MAE
```

validation / test：

```text
inverse transform
-> raw-scale MAE / RMSE / MAPE
```

聚合必须按总和：

\[
MAE
=
\frac{\sum SAE}{\sum valid}
\]

\[
RMSE
=
\sqrt{
\frac{\sum SSE}{\sum valid}
}
\]

禁止：

```text
average(batch RMSE)
```

MAPE 只对 raw target 中有效且非近零位置统计。

每个 horizon 单独累计。

必须有：

```text
BasicTS MAE parity
BasicTS RMSE parity
BasicTS MAPE parity
```

---

# 7. Phase 0 —— BasicTS + STID Runtime Sanity

HiDFilter 实现前先完成此阶段。

## 7.1 目的

判断：

```text
data
scaler
mask
metrics
DataLoader
Ascend runtime
optimizer loop
checkpoint
```

是否健康。

---

## 7.2 STID 配置

Phase 0 优先使用 BasicTS 官方/成熟 STID。

要求：

- PEMS08；
- traffic-only；
- FP32；
- 相同 split；
- 相同 scaler/mask；
- 相同 raw-scale metrics；
- 相同 Ascend 设备；
- 合理 batch size。

不需要做完整 STID 超参搜索。

---

## 7.3 Phase 0 必须记录

```text
train seconds / epoch
validation seconds
total epoch scope
samples / second
peak NPU memory
AICore utilization if available
data wait
first epoch train loss
validation MAE
```

---

## 7.4 Phase 0 通过条件

至少：

1. loss 正常下降；
2. validation MAE 数值合理；
3. 无 NaN/Inf；
4. NPU 不存在明显长时间 idle；
5. train/validation 时间边界清楚；
6. 数据与 metrics golden tests 全通过；
7. checkpoint save/load 正常。

如果 STID 都异常慢：

> 先修 BasicTS→Ascend runtime。

禁止继续写 HiDFilter 来掩盖基础设施问题。

---


## 7.5 Phase 0 还必须建立 8 卡并行吞吐基线

除了单 run 性能，还需要验证整机并行实验效率。

Phase 0 在单卡 STID 正常后，追加一个短时并行 smoke：

```text
至少 4 张卡
推荐 8 张卡
每卡 1 个独立短 run
```

目标不是比较模型精度，而是检查：

- 8 个进程能否稳定同时启动；
- 是否发生 CPU / 内存 / 磁盘 I/O 争抢；
- DataLoader 是否因并行任务过多而变慢；
- 单卡吞吐在 8-job 并发时是否显著下降；
- 是否存在某些 NPU / PCIe / NUMA 路径异常；
- checkpoint/logging 是否产生共享目录争用。

必须比较：

```text
single-job samples/s
4-job aggregate samples/s
8-job aggregate samples/s

single-job per-run samples/s
8-job per-run samples/s
```

如果 8-job 并行时每个 job 吞吐严重下降，优先检查：

```text
CPU oversubscription
DataLoader workers
shared storage bandwidth
H2D
NUMA locality
logging/checkpoint I/O
```

不要错误地把这种整机资源竞争归因于 HiDFilter 模型。


# 8. HiDFilter 总体结构

模型只包含一条主逻辑：

```text
history
   |
   +------------------------------+
   |                              |
   v                              v
Target Context              Lag Content
   |                              |
   |                    +---------+---------+
   |                    |                   |
   v                    v                   v
Family Router       Family Evidence      Fine Lag Token
   |                                        |
Family Top-p                           Fine Sensor-Lag
   |                                   Softmax + Top-p
   |                                        |
   +-------------------+--------------------+
                       |
                       v
               Hierarchical Fusion
                       |
                       v
                    Decoder
                       |
                       v
                 prediction
```

核心层次概率：

\[
P(r,j,\tau\mid X,i,h)
=
P(r\mid c_i,u_i^r,e_i,e_h)
\cdot
P(j,\tau\mid r,c_i,z_{j,\tau},e_i,e_h,\pi_{ij}^r)
\]

其中：

```text
r ∈ {Self, Physical, Semantic}
```

三个 family 是 candidate dependency spaces，不是 forecasting experts。

---

# 9. 默认模型维度

| 参数 | 默认值 |
|---|---:|
| History L | 12 |
| Horizon H | 12 |
| Families R | 3 |
| Hidden D | 64 |
| Node Embedding | 16 |
| Lag Embedding | 16 |
| Horizon Embedding | 16 |
| Family Embedding | 16 |
| Kp | 8 |
| Ks | 8 |
| rho_family | 0.8 |
| rho_edge | 0.8 |
| Dropout | 0.1 |

Family ID：

```text
0 Self
1 Physical
2 Semantic
```

---

# 10. Lag 索引

对外统一：

\[
\tau=1,\ldots,12
\]

含义：

```text
tau=1  latest
tau=12 oldest
```

代码：

\[
lag\_index=\tau-1
\]

history：

```text
[B,12,N,1]
oldest -> newest
```

映射：

\[
x^{lag}_{lag\_index}
=
history[:,11-lag\_index]
\]

测试：

```text
tau=1  -> history[:,11]
tau=12 -> history[:,0]
```

---

# 11. Candidate Flatten Order

固定：

\[
edge\_index
=
sensor\_rank\times12+lag\_index
\]

sensor-major / lag-minor。

例如：

```text
sensor_rank 0:
tau 1..12

sensor_rank 1:
tau 1..12
...
```

该顺序同时用于：

- gather metadata；
- diagnostics；
- Edge Top-p tie break。

---

# 12. Candidate Space

每个 target：

```text
Self:
1 source * 12 lags = 12

Physical:
Kp=8 * 12 = 96

Semantic:
Ks=8 * 12 = 96
```

最大：

\[
204
\]

candidate source-lag positions。

Candidate 全部 offline build + cache。

模型 forward 不允许在线构图。

---

# 13. Graph Contract

每个 dataset config 显式指定：

```yaml
graph_mode: directed | undirected
weight_semantics: affinity | distance | cost
conversion_scale: null | positive_number
```

禁止根据数值自动猜。

Adjacency：

```text
A[src,dst] > 0
```

diagonal 忽略。

---

## 13.1 Affinity

正边：

```text
A / positive_max
```

没有正边：

```text
Physical unavailable
```

---

## 13.2 Distance / Cost

必须：

```text
conversion_scale > 0
```

转换：

\[
w_{uv}
=
\exp
\left[
-
\left(
\frac{A_{uv}}{conversion\_scale}
\right)^2
\right]
\]

再对正权重做 max normalize。

禁止 runtime 自动估计 scale。

---

# 14. Physical Candidate

目标 \(i\)。

原始 one-hop：

\[
\mathcal N_i^{1hop}
=
\{j\ne i:
A[j,i]>0 \lor A[i,j]>0
\}
\]

按四阶段：

### Stage 1

```text
direct incoming / bidirectional
```

### Stage 2

```text
direct outgoing-only
```

### Stage 3

```text
remaining node with directed path
j -> ... -> i
```

### Stage 4

```text
remaining connected node in undirected projection
```

若 undirected：

```text
all direct neighbors -> Stage 1
Stage 2 empty
```

stage 内排序：

```text
shortest_hop ascending
path_strength descending
sensor_index ascending
```

最短路径 strength：

\[
path\_strength
=
\max_{shortest\ paths}
\prod w_{uv}
\]

排序计算使用：

```text
CPU float64 log-space
```

最终取：

\[
K_p=8
\]

不足：

```text
index=0
valid=false
prior=0
```

禁止 disconnected refill。

---

# 15. Semantic Candidate

只允许使用：

```text
raw train traffic
original one-hop mask
final Physical candidates
```

禁止 val/test traffic。

---

## 15.1 First Difference

\[
\Delta x_{t,j}
=
x_{t,j}-x_{t-1,j}
\]

只有两个 adjacent raw value 都 valid 时，该 difference 才 valid。

---

## 15.2 Pair-specific correlation

每个 sensor pair 使用：

```text
common-valid first-difference samples
```

Pearson correlation。

规则：

```text
min_overlap = 288
variance <= 1e-12 -> unavailable
CPU float64 sufficient statistics
clip correlation [-1,1]
store/rank float32
```

禁止 Python 双循环遍历 sensor pair。

---

## 15.3 排除集合

Semantic 合法 source：

\[
V
\setminus
(
\{i\}
\cup
N_i^{1hop}
\cup
N_i^{phy}
)
\]

必须排除：

- self；
- original one-hop；
- final Physical candidate。

---

## 15.4 Ranking

```text
abs(correlation) descending
sensor_index ascending
```

取：

\[
K_s=8
\]

prior：

\[
\pi^{sem}_{ij}=|corr_{ij}|
\]

signed correlation 单独保存。

不足：

```text
padding
no refill
```

---

# 16. Candidate Artifact

至少保存：

```text
physical_index          int64   [N,Kp]
physical_valid          bool    [N,Kp]
physical_prior          float32 [N,Kp]
physical_hops           int16   [N,Kp]
physical_log_strength   float64 [N,Kp]

semantic_index          int64   [N,Ks]
semantic_valid          bool    [N,Ks]
semantic_prior          float32 [N,Ks]
semantic_corr_signed    float32 [N,Ks]

original_one_hop        bool    [N,N]
family_available        bool    [N,3]
```

NPZ deterministic：

```text
.npy v1.0
ZIP_STORED
canonical entry order
allow_pickle=False
```

manifest 保存：

```text
schema_version
algorithm_version
dataset
N
train_length
Kp/Ks
graph_mode
weight_semantics
conversion_scale
null tolerance
semantic min overlap
train traffic SHA256
adjacency SHA256
BasicTS version
```

mismatch：

```text
reject
```

模型 buffer：

```python
persistent=False
```

---

# 17. Target Context Encoder

输入：

```text
history [B,12,N,1]
```

每个 sensor 独立。

先：

```text
Linear(1,D)
```

再两层 causal residual Conv1d：

```text
kernel=3
dilation=1
dilation=2
GELU
dropout=0.1
residual
each block own LayerNorm
```

block：

\[
H'
=
LN
(
H
+
Dropout(GELU(CausalConv(H)))
)
\]

最终：

\[
c_i
=
LN
\left(
H_{latest}
+
Mean_t^{FP32}(H_t)
\right)
\]

输出：

```text
context [B,N,D]
```

Context Encoder 不做 spatial aggregation。

---

# 18. Lag Content Encoder

lag 必须独立编码。

\[
\ell_{j,\tau}
=
MLP_{lag}(x_{j,\tau})
+
P_l(e_\tau^{lag})
\]

其中：

```text
MLP:
Linear(1,D)
GELU
Linear(D,D)
```

lag embedding：

```text
12 x 16
P_l: Linear(16,D)
```

输出：

```text
lag_content [B,N,12,D]
```

禁止：

- temporal convolution；
- temporal attention；
- temporal pooling；
- source node embedding；
- family embedding；
- prior；
- candidate identity；
- timestamp。

---

# 19. Fine Lag Token

Fine path：

\[
z_{j,\tau}
=
LN
(
\ell_{j,\tau}
+
P_n(e_j^{node})
)
\]

node embedding：

```text
[N,16]
```

projection：

```text
P_n: Linear(16,D)
```

输出：

```text
lag_token [B,N,12,D]
```

同一个 node embedding / \(P_n\) 用于：

- source identity；
- Fine target query identity。

---

# 20. Family Evidence

主模式：

```text
family_evidence
```

消融模式：

```text
target_only
```

概念定义：

\[
u_i^r
=
Mean_{(j,\tau)\in C_i^r}
\ell_{j,\tau}
\]

要求：

```text
parameter-free
unweighted
FP32 reduction
candidate-order invariant
no prior
no node identity
no Fine score
no Top-p
empty -> exact zero
```

---

## 20.1 Evidence 高效实现

禁止：

```text
[B,N,K,L,D]
```

先：

\[
\bar\ell_j
=
Mean_\tau^{FP32}(\ell_{j,\tau})
\]

得到：

```text
[B,N,D]
```

再 candidate sensor gather：

```text
[B,N,K,D]
```

最后 masked sensor mean。

Self：

```text
own sensor lag mean
```

---

# 21. Family Availability

\[
A_i=
[
True,
any(physical_valid_i),
any(semantic_valid_i)
]
\]

availability 在 Router softmax 前处理。

Unavailable family：

```text
prob=0
weight=0
keep=false
message contribution=0
```

---

# 22. Shared Family Router

输入：

\[
[
c_i
\Vert
e_i^{node}
\Vert
e_h^{hor}
\Vert
u_i^r
\Vert
e_r^{route}
]
\]

如果：

```text
router_mode=target_only
```

则：

\[
u_i^r=0
\]

共享 scalar scorer：

```text
Linear(2D + Dn + Dh + Df, D)
GELU
Linear(D,1)
```

每个 family 使用同一组参数。

输出：

```text
router_logits [B,N,H,3]
router_prob_dense [B,N,H,3]
```

Router 可以读取：

- target context；
- target identity；
- horizon；
- pooled family evidence；
- route-family embedding。

Router 不允许读取：

- candidate index；
- candidate source embedding；
- candidate prior；
- Fine score；
- Fine probability；
- Edge Top-p；
- family message。

---

# 23. Family Top-p

\[
\rho_f=0.8
\]

Router dense probability 上执行。

warm-up 时不开 hard Top-p。

正常：

```text
support = TopP(router_prob.detach())
router_weight = exact renormalize(original router_prob)
```

Family canonical order：

```text
Self
Physical
Semantic
```

---

# 24. Fine Query

\[
q_{i,h}
=
W_q
LN
(
c_i
+
P_n(e_i)
+
P_h(e_h)
)
\]

horizon embedding：

```text
12 x 16
```

\[
P_h:16\rightarrow D
\]

输出：

```text
Q [B,N,H,D]
```

只允许一个 \(W_q\)。

---

# 25. Global Fine K/V

这是性能关键硬规则。

先全局构造：

\[
Z
=
LN
(
lag\_content
+
source\_node\_projection
)
\]

```text
Z [B,N,L,D]
```

Value：

\[
V=W_vZ
\]

只计算一次：

```text
V_global [B,N,L,D]
```

每个 family：

\[
K^r
=
W_k
(
Z
+
P_f(e_r^{fine})
)
\]

```text
K_global_r [B,N,L,D]
```

只有：

```text
one W_k
one W_v
```

强制顺序：

```text
global token
-> global projection
-> candidate gather
```

禁止：

```text
candidate gather
-> LayerNorm
-> Linear
```

---

# 26. Static Gather Metadata

初始化时预计算：

```text
source_index
lag_index
source_lag_flat_index
valid
prior
```

每 family：

```text
[N,C_r]
```

其中：

```text
C_self=12
C_phy=96
C_sem=96
```

\[
source\_lag\_flat\_index
=
source\_index\times12+lag\_index
\]

global K/V：

```text
[B,N*12,D]
```

target candidate gather：

```text
[B,N,C_r,D]
```

forward 不重复：

- flatten；
- expand；
- range check；
- prior check；
- validity check。

---

# 27. Fine Score

Family \(r\)：

\[
s^r_{i,h,j,\tau}
=
\frac{q^\top k^r}{\sqrt D}
+
\alpha
\log
(
\max(\pi_{ij}^r,10^{-6})
)
\]

\[
\alpha=Softplus(\alpha_{raw})
\]

全模型只有一个：

```text
alpha_raw
```

Self、Physical、Semantic 共享同一个 \(\alpha\)，禁止创建
`alpha_self`、`alpha_phy`、`alpha_sem` 或对应的多套 raw 参数。

初始：

\[
\alpha=1
\]

Self prior：

```text
1
```

Physical prior：

```text
path affinity
```

Semantic prior：

```text
abs(train corr)
```

prior 只影响 Fine score。

---

# 28. Safe FP32 Masked Softmax

统一 primitive。

必须：

```text
invalid prob = exact 0
valid row sum = 1
all-invalid row -> all zero
FP32
```

禁止对 all-invalid row 直接普通 softmax。

strict/debug：

```text
valid NaN/Inf -> error
```

禁止 `nan_to_num` 掩盖问题。

---

# 29. Family-wise Fine Softmax

每 family 独立：

\[
a^r
=
Softmax(C_i^r)
\]

禁止把所有 family candidate union 以后统一 softmax。

---

# 30. Deterministic Top-p

Family 和 Edge 共用。

合法：

\[
0<\rho\le1
\]

若：

```text
rho=1
```

保留全部 valid。

support：

\[
K_\rho
=
\min
\left\{
k:
\sum_{l=1}^{k}p_{(l)}
\ge\rho
\right\}
\]

crossing item 保留。

FP32 tolerance：

\[
\delta_n
=
4(n+1)\epsilon_{32}
\]

判断：

\[
C_k+\delta_n\ge\rho
\]

---

## 30.1 Tie Rule

detached FP32 probability 决定：

- support size；
- cutoff。

然后：

```text
p > cutoff -> keep
p == cutoff -> 按 canonical index 填 quota
```

Edge canonical：

\[
sensor\_rank\times12+lag\_index
\]

最终：

```text
support detached
weight from original non-detached probability
exact positive renormalization
```

禁止 denominator `+ eps`。

---

# 31. Edge Top-p

\[
\rho_e=0.8
\]

每个：

```text
B x target x H x family
```

独立执行。

允许同 source：

```text
lag1
lag4
lag7
```

同时保留。

禁止 main model 做：

```text
argmax lag per source
```

---

# 32. Family Message

\[
m^r_{i,h}
=
\sum_{j,\tau}
\tilde a^r_{i,h,j,\tau}
v_{j,\tau}
\]

shape：

```text
[B,N,H,D]
```

---

# 33. Hierarchical Fusion

\[
m_{i,h}
=
\sum_r
\tilde r_{i,h,r}
m^r_{i,h}
\]

完整 joint coefficient：

\[
A^r_{i,h,j,\tau}
=
\tilde r_{i,h,r}
\tilde a^r_{i,h,j,\tau}
\]

active valid hierarchy 上 joint coefficient 总和应为 1。

---

## 33.1 Memory Rule

正常训练建议：

```text
fused_message [B,N,H,D]
```

逐 family 累加。

不要求长期保留：

```text
[B,N,H,3,D]
```

diagnostics 才按需保存额外内容。

---

# 34. Decoder

输入：

\[
[
c_i
\Vert
m_{i,h}
\Vert
e_h
]
\]

网络：

```text
Linear(2D + Dh,D)
GELU
Dropout(0.1)
Linear(D,1)
```

默认：

```text
residual
```

\[
\hat y
=
x_{latest}
+
f
\]

备选：

```text
direct
```

\[
\hat y=f
\]

一个 decoder。

---

# 35. Decoder Selection

只允许 PEMS08 train/val。

seed：

```text
42
43
44
```

比较 residual vs direct。

Direct 只有在：

```text
3/3 seed 全部更好
AND
mean relative validation MAE improvement >= 0.5%
```

才成为全局 decoder。

否则 residual。

test 不参与。

---

# 36. Loss

Prediction：

\[
L_{pred}
=
\frac{
\sum V|\hat y-y|
}{
\sum V
}
\]

FP32。

zero-valid train batch：

```text
skip before forward
```

---

# 37. Router Balance

使用 dense Router probability。

\[
u_r
=
\frac1{Q_v}
\sum_qV_qr_{q,r}
\]

\[
t_r
=
\frac1{Q_v}
\sum_q
V_q
\frac{A_{q,r}}{\sum_sA_{q,s}}
\]

\[
L_{bal}
=
\sum_{r:t_r>0}
\frac{(u_r-t_r)^2}{t_r+10^{-8}}
\]

总损失：

\[
L=L_{pred}+10^{-3}L_{bal}
\]

如果只有一个 available family：

```text
balance = 0
```

---

# 38. Optimizer

```text
AdamW
lr=1e-3
betas=(0.9,0.999)
eps=1e-8
weight_decay=1e-4
grad_clip=5.0
no gradient accumulation
```

分组：

```text
ndim >= 2 -> decay
ndim < 2  -> no decay
```

---

# 39. LR Schedule

100 epochs。

\[
lr_e
=
10^{-5}
+
0.5(10^{-3}-10^{-5})
\left(
1+\cos(\pi e/99)
\right)
\]

```text
epoch_index=0 -> 1e-3
epoch_index=99 -> 1e-5
```

无 LR warm-up。

---

# 40. Family Top-p Warm-up

```text
first 5 displayed epochs
family hard Top-p OFF
```

即：

```text
epoch_index 0..4
apply_family_top_p=false
```

从：

```text
epoch_index=5
```

开始 true。

warm-up train/val 一致。

---

# 41. Early Stopping

```text
patience=15
min_delta=0
monitor=raw-scale average validation MAE
```

warm-up val：

```text
log only
not best eligible
not patience eligible
```

第 6 displayed epoch 第一次参与 best。

tie：

```text
earlier checkpoint wins
```

---

# 42. Forward API

固定：

```python
forward(
    history,
    *,
    timestamps=None,
    apply_family_top_p: bool,
    collect_diagnostics: bool=False,
)
```

model 不知道：

- epoch；
- optimizer；
- candidate file path；
- NPU device；
- scaler；
- run stage。

这些都属于 Runner。

---

# 43. Ascend 边界

model package：

```text
pure PyTorch
```

禁止：

- `torch_npu`；
- `.cuda()`；
- CUDA string；
- NPU/CUDA branches；
- PyG；
- DGL；
- torch_scatter；
- custom CUDA ops。

runtime/accelerator 才能 lazy import `torch_npu`。

第一阶段：

```text
FP32 only
AMP disabled
```

---


## 43.1 当前 8×910B4 的设备使用规则

针对当前 8 张 Ascend 910B4：

### 单卡显存安全线

正式实现不要按 32768 MB 极限顶满。

初始工程安全目标：

```text
peak process HBM <= 26 GB
```

若 profile 显示 runtime/system reserve 更稳定，也可以放宽，但任何正式配置都应至少保留约 10%~15% HBM 余量。

如果单 run 峰值超过：

```text
~28 GB
```

应先检查：

- 禁止的大 tensor 是否被 materialize；
- diagnostics 是否保留了不必要 tensor；
- autograd graph 是否意外长期持有；
- batch size 是否合理；

而不是首先使用多卡拆模型。

### Device 显式选择

每个 run 必须：

- 显式指定一个 NPU device；
- 日志记录 physical/logical device id；
- 记录 run 开始时的 `npu-smi` 设备状态；
- 禁止两个正式训练 run 无意落到同一张卡。

launcher 应按照当前已安装 CANN / torch_npu 支持的可见设备机制设置设备隔离。

不要在规格里猜一个固定环境变量名；Codex 在 Phase 0 必须实际检测当前环境支持的方式，并封装到统一 launcher。

### 不允许隐式多卡

模型代码不得因为机器有 8 张卡就自动初始化 distributed。

默认：

```text
world_size = 1
```

只有显式 distributed 配置才允许多卡训练。


# 44. Performance-Critical Invariants

这是本项目非常重要的实现要求。

---

## 44.1 禁止 `[B,N,H,C,D]`

允许：

```text
Q [B,N,H,D]
K [B,N,C,D]
V [B,N,C,D]
score [B,N,H,C]
```

推荐：

```python
score = einsum("bnhd,bncd->bnhc")
message = einsum("bnhc,bncd->bnhd")
```

---

## 44.2 禁止 `[B,N,K,L,D]`

Evidence：

```text
lag mean before gather
```

Fine：

```text
flattened global K/V before gather
```

---

## 44.3 Projection Before Gather

必须：

```text
lag_content
-> global lag_token
-> global K/V
-> target-specific candidate gather
```

禁止：

```text
candidate gather
-> token normalization
-> K/V projection
```

---

## 44.4 Static Metadata Precompute

初始化：

```text
flatten index
lag index
valid
prior
availability
```

forward 不重复计算。

---

## 44.5 禁止热路径大量 `.item()`

静态 invariant 禁止每 batch：

```text
all().item()
any().item()
```

严格检查在初始化或 debug。

---

## 44.6 禁止 Validation Full Tensor CPU Transfer

大 tensor：

```text
edge prob / keep
```

在 NPU reduce。

CPU 只接：

```text
scalar
small vectors
12-bin histogram
```

---

# 45. Data Pipeline 性能

split 加载后：

```text
raw validity once
normalization once
```

Dataset 只 slice。

禁止 `__getitem__` 每次：

```text
recompute mask
recompute scaler
large copy
```

禁止提前展开全部 sliding windows。

---

# 46. 初始 Batch / Seed

PEMS08：

```text
train_batch_size = 64
eval_batch_size = 64
seed = 42
shuffle_train = true
shuffle_eval = false
drop_last = false
```

每 epoch：

\[
shuffle\_seed=seed+epoch\_index
\]

---


## 46.1 8 卡实验调度策略

为了最大化 wall-clock 实验效率，8 张卡主要用于 **run-level parallelism**。

### 开发期

在模型尚未稳定时，不建议同时放飞大量不同代码版本。

推荐：

```text
NPU0: 当前主开发 smoke
NPU1: STID / protocol regression
NPU2: profiler / strict-debug comparison
NPU3: fixed-batch overfit / numerical regression
NPU4~7: 根据当前 Phase 运行受控对照，或保持空闲
```

当当前 Phase 通过 correctness gate 后，再扩大并行。

### Decoder Selection

Residual / Direct × seeds 42/43/44：

```text
6 independent runs
```

建议一次并行：

```text
NPU0 seed42 residual
NPU1 seed43 residual
NPU2 seed44 residual
NPU3 seed42 direct
NPU4 seed43 direct
NPU5 seed44 direct
NPU6 regression
NPU7 profiler / reserve
```

因此 decoder selection 可以接近一轮 wall-clock 完成，而不是串行跑 6 次。

### PEMS08 正式三 seed

```text
NPU0 seed42
NPU1 seed43
NPU2 seed44
```

其余卡可以并行运行已经冻结实现的关键 ablation。

### Ablation

8 卡可以直接把 8 个主要对照分配成一波：

```text
NPU0 Flat
NPU1 Sensor-only
NPU2 Single-Lag
NPU3 matched Top-K
NPU4 target_only Router
NPU5 w/o prior
NPU6 w/o Physical / 当前最重要 family removal
NPU7 profiler / reproducibility repeat
```

实际实验组合以当时冻结的实验清单为准。

### 六数据集扩展

最终配置冻结后，可以按 dataset/run 粒度并行。

不要让一个模型 run 用 8 卡 DDP，同时让其它 7 张卡闲置。

### 每个并行 run 必须独立

必须独立：

```text
run_id
output directory
log
checkpoint directory
seed
device id
temporary files
```

共享：

```text
read-only dataset
read-only candidate cache
read-only config template
```

candidate artifact 应预先构建一次，禁止 8 个 run 同时争抢生成同一 cache。


# 47. Diagnostics

只有 validation / analysis 需要。

effective edge：

```text
edge_keep
AND family_keep
AND candidate_valid
```

最终论文以外的实验分析也必须使用 effective dependency，不用单独 edge_keep 冒充最终选择结果。

---

## 47.1 Family Stats

```text
availability
activation conditioned on availability
dense usage
sparse usage
active family count
router entropy
retained family mass
```

---

## 47.2 Edge Stats

```text
effective edge count
edge retained mass
lag histogram tau=1..12
multi-lag numerator
multi-lag denominator
multi-lag ratio
```

---

## 47.3 Multi-Lag

denominator：

```text
target-horizon-source
with >=1 selected effective lag
```

numerator：

```text
same group with >=2 selected effective lags
```

---

# 48. Performance Profiler

每个 Phase 必须记录：

```text
data_wait_ms
h2d_ms
forward_ms
context_ms
family_evidence_ms
router_ms
fine_filter_ms
edge_top_p_ms
backward_ms
optimizer_ms
validation_forward_ms
diagnostics_ms
checkpoint_ms
samples_per_second
peak_memory
AICore utilization if available
```

所有计时要明确：

```text
是否 synchronize
timer scope
train-only or full epoch
```

---


## 48.1 8 卡机器还必须监控“单 run 性能”和“整机实验吞吐”

性能报告分成两类。

### A. 单 run latency / throughput

用于判断模型实现是否高效：

```text
step time
samples/s
forward/backward
AICore
peak HBM
```

### B. aggregate experiment throughput

用于判断整机是否被充分利用：

```text
active NPU count
aggregate samples/s
jobs completed / hour
per-job throughput degradation under concurrency
```

不能只追求单个 run 最快。

在拥有 8 张 910B4 的情况下，实验阶段更重要的优化目标通常是：

\[
\boxed{
\text{total experiments completed per wall-clock hour}
}
\]

例如：

```text
1 个 run 速度提升 15%
```

通常不如：

```text
6~8 个独立 run 稳定并行
```

带来的整体实验周期缩短明显。

### 外部 NPU 监控

建议 runtime 提供独立 monitor，不从 model forward 采集设备利用率。

每隔约：

```text
5~10 seconds
```

记录：

```text
timestamp
NPU id
AICore %
HBM usage
power
temperature
process id
```

写入 run-specific CSV/JSONL。

监控本身不得触发模型 tensor 同步。


# 49. Checkpoint

保存：

```text
run_id
schema
completed_epoch
global_step
model
optimizer
scheduler
scaler stats
early stopping
metric history
Python RNG
NumPy RNG
Torch CPU RNG
Torch NPU RNG
next epoch index
fingerprints
environment
```

candidate buffer：

```text
persistent=False
```

不重复存 checkpoint。

---

# 50. Resume

```text
build objects
-> validate fingerprints
-> load model/optimizer/scheduler
-> restore RNG last
-> start next epoch
```

```text
last -> resume
best -> evaluation
```

继续 best：

```text
new experiment
```

---

# 51. Fingerprint

Execution identity：

```text
src
tests
configs
scripts
pyproject
environment declarations
```

Git provenance 单独记录：

```text
commit
branch
dirty
diff hash
```

不要把 docs-only Git commit 变化当成模型证据无效。

---

# 52. Run Stage

```text
development
final
```

Development：

```text
train + val
no test
```

Final：

```text
frozen config
train/val selection
reload best
test once
```

---

# 53. 增量实现计划

---

## Phase 0：BasicTS + STID

目的：

```text
validate infrastructure
```

通过后再写 HiDFilter。

---

## Phase 1：Self-only

实现：

```text
Context
Lag Content
Fine token
Q/K/V
Self Softmax
Self Edge Top-p
Decoder
```

要求：

```text
loss decreases
fixed-batch overfit
CPU/NPU step works
speed normal
```

---

## Phase 2：Physical

加入：

```text
Physical candidate
Physical Fine path
```

Router 暂不开。

记录增量时间。

---

## Phase 3：Semantic

加入：

```text
Semantic builder
Semantic Fine path
```

必须证明 train-only。

---

## Phase 4：Full Fine

三个 family Fine 全开。

验证：

```text
family-wise softmax
Top-p
multi-lag
empty family
```

---

## Phase 5：Family Evidence Router

加入：

```text
evidence
availability
shared Router
dense family weighting
```

hard Family Top-p 暂不开。

---

## Phase 6：Family Top-p

加入：

```text
Family Top-p
warm-up
hierarchical fusion
```

完整跑 >=6 epochs。

---

## Phase 7：Diagnostics

加入：

```text
device-side aggregation
```

检查 diagnostics overhead。

---

## Phase 8：Decoder Selection

PEMS08：

```text
3 seeds
residual/direct
```

完成后冻结 decoder。

---

## Phase 9：PEMS08 正式效果验证

完成：

```text
seed 42 / 43 / 44
full training
best validation
formal test
```

先确认：

- 效果；
- 稳定性；
- seed 方差；
- 速度；
- peak memory。

---

## Phase 10：关键结构实验

按优先级：

```text
Hierarchical vs Flat
Sensor-Lag vs Sensor-only
Multi-Lag vs Single-Lag
Top-p vs matched Top-K
target_only Router
```

只有这些结果合理后再扩全部数据集。

---

## Phase 11：扩展六数据集

建议顺序：

```text
PEMS08
PEMS04
PEMS07
PEMS03
METR-LA
PEMS-BAY
```

PEMS07 重点检查：

```text
runtime
memory
candidate construction
```

---

# 54. Correctness Gate

任何 Phase 必须通过：

```text
shape
no NaN
forward
backward
loss finite
gradient finite
fixed-batch optimization
```

涉及 Top-p：

```text
CPU/NPU support parity
```

涉及 Candidate：

```text
determinism
no leakage
padding
exclusion
```

---

# 55. Performance Gate

禁止进入下一 Phase，如果出现：

```text
unexplained >2x step time
AICore near idle
host sync excessive
large tensor materialization
full validation tensor -> CPU
memory abnormal
```

Phase 之间必须有 timing diff。

---

# 56. Mandatory Tests —— Protocol

```text
window counts
no crossing
scaler
raw mask
inverse transform
MAE parity
RMSE parity
MAPE parity
zero-valid
timestamp isolation
```

---

# 57. Mandatory Tests —— Candidate

```text
graph formats
weight semantics
conversion
directed / undirected
Physical stage ranking
path tie
padding
Semantic common-valid correlation
min overlap
constant sensor
Self exclusion
one-hop exclusion
Physical exclusion
train-only
cache hash
deterministic serialization
```

---

# 58. Mandatory Tests —— Top-p

必须：

```text
[.8,.1,.1], rho=.8 -> 1
[.45,.35,.20], rho=.8 -> 2
[.4,.3,.3], rho=.8 -> 3
```

以及：

```text
rho=1
invalid rho
all invalid
tie
FP32 tolerance
canonical order
single support
CPU/NPU parity
```

---

# 59. Mandatory Tests —— Evidence / Router

Evidence：

```text
parameter-free
permutation invariant
no prior
no source identity
empty zero
optimized mean equivalent
```

Router：

```text
shared scorer
availability
prior insensitive
Fine insensitive
source identity boundary
horizon sensitivity
target_only schema
```

---

# 60. Mandatory Tests —— Fine

```text
one Q/K/V
one decoder
multi-lag
empty family
candidate padding
probability normalization
hierarchical joint normalization
```

---

# 61. Mandatory Tests —— Runtime

```text
CPU full step
NPU full step
overfit fixed batch
grad clip
LR sequence
warm-up
early stopping
checkpoint best reload
resume
model source guard
```

---

# 62. 预期效果的判断顺序

“效果差”不能直接归因于 idea。

必须按顺序诊断。

---

## 62.1 Baseline 正常吗？

如果 STID：

```text
速度异常
指标异常
```

先修 infrastructure。

---

## 62.2 Self-only 正常吗？

Self-only 应至少能：

```text
loss 降
overfit tiny batch
validation 不崩
```

否则先查：

- lag mapping；
- target mapping；
- scaler；
- decoder；
- optimizer。

---

## 62.3 加 Physical 后是否恶化？

如果明显恶化：

查：

```text
candidate ranking
prior
valid mask
gather mapping
softmax
Top-p
```

---

## 62.4 加 Semantic 后是否恶化？

查：

```text
correlation
leakage
one-hop exclusion
prior scale
empty/padding
```

---

## 62.5 Router 后是否 collapse？

查：

```text
availability
balance
warm-up
family evidence
Top-p
```

不要立刻增加新模块。

---

# 63. 主要实验配置

第一阶段固定：

```text
D=64
Kp=8
Ks=8
rho_family=.8
rho_edge=.8
dropout=.1
AdamW
lr=1e-3
weight_decay=1e-4
100 epochs
patience=15
warmup=5 epochs
FP32
```

除非主实现确定无 bug，否则不允许同时大范围调超参。

---

# 64. Decoder Selection 后的正式默认配置

一旦 decoder 选定：

```text
freeze decoder_mode
```

后续数据集不得按结果临时改。

---

# 65. 关键结构对照实验

这些实验用于验证模型设计是否真的有效。

---

## 65.1 Hierarchical vs Flat

Hierarchical：

```text
family routing
+
family-wise Fine
```

Flat：

```text
all candidate union
-> one Fine distribution
```

其他参数尽量一致。

---

## 65.2 Sensor-Lag vs Sensor-only

Sensor-only：

```text
12 lags pool per source
-> source score
```

Sensor-Lag：

```text
each (source,lag) independently scored
```

---

## 65.3 Multi-Lag vs Single-Lag

Single-Lag：

```text
max one lag per source
```

Multi-Lag：

```text
multiple lags allowed
```

---

## 65.4 Top-p vs Top-K

比较时：

```text
matched average selected edge count
```

不能让 Top-p 只是因为选择更多 edge 占优势。

---

## 65.5 Router family_evidence vs target_only

相同 schema。

target_only：

```text
u_i^r = 0
```

从头训练。

---

# 66. 实验分析输出

至少保存：

```text
family availability
family activation
active family count
router entropy
family retained mass
edge retained mass
effective edge count
lag histogram
multi-lag ratio
```

所有 selected edge 统计使用：

```text
effective_edge_keep
```

---

# 67. 性能目标

不预设硬性：

```text
必须 40s/epoch
```

但必须做到：

1. STID runtime 正常；
2. HiDFilter 每个 Phase 的增量成本可解释；
3. 不出现明显 host sync bottleneck；
4. 不出现重复 K/V projection；
5. 不出现巨大不必要 intermediate；
6. diagnostics 不成为 epoch 主耗时；
7. PEMS08 NPU utilization 明显高于“多数时间 idle”的状态。

如果完整模型仍然慢：

```text
profile first
optimize second
```

不能凭猜测优化。

---


## 67.1 当前硬件下“拉满实验效率”的优先级

针对当前 8×910B4，性能优化优先顺序固定为：

### P0：保证单卡实现没有明显浪费

必须先解决：

```text
重复 K/V projection
禁止的大 tensor
频繁 host sync
validation full tensor D2H
DataLoader 明显等待
```

### P1：使用 8 卡并行独立实验

当单卡 run 正常后：

```text
seed parallelism
decoder candidate parallelism
ablation parallelism
dataset parallelism
```

这是缩短总实验周期的主要手段。

### P2：CPU / I/O 并发调优

8-job 模式下重点 profile：

```text
CPU usage
RAM
DataLoader workers
disk read bandwidth
checkpoint write contention
NUMA
H2D
```

`num_workers` 不按单卡最优值机械乘 8。

如果每个 job 都开很多 workers，可能造成 CPU oversubscription。

### P3：单 run batch-size 吞吐测试

正式训练 batch size 初始保持：

```text
64
```

因为 batch size 会影响优化动态。

允许在 **纯性能 microbenchmark** 中测试：

```text
64
128
256
...
```

寻找 NPU 吞吐拐点，但不得自动把 throughput 最快的 batch size 改成正式实验 batch。

如果要修改正式 batch size，必须单独批准并重新冻结：

- optimizer；
- LR；
- baseline comparability；
- training protocol。

### P4：AMP

FP32 correctness/performance gate 完成后，才允许单独测试 Ascend AMP/BF16/FP16。

AMP 是后续 runtime 优化，不得在初始实现阶段同时引入。

### P5：DDP

最后才考虑。

只在：

```text
单 run 确实需要
```

时启用，不为了“机器有 8 卡”而启用。


# 68. 初始目录建议

```text
HiDFilter/
├── README.md
├── pyproject.toml
├── configs/
│   ├── base.yaml
│   └── datasets/
│       ├── PEMS03.yaml
│       ├── PEMS04.yaml
│       ├── PEMS07.yaml
│       ├── PEMS08.yaml
│       ├── METR-LA.yaml
│       └── PEMS-BAY.yaml
├── src/hidfilter/
│   ├── config.py
│   ├── protocol/
│   │   ├── basic_ts_adapter.py
│   │   ├── data.py
│   │   └── metrics.py
│   ├── baselines/
│   │   └── stid_adapter.py
│   ├── candidates/
│   │   ├── graph.py
│   │   ├── physical.py
│   │   ├── semantic.py
│   │   └── artifact.py
│   ├── model/
│   │   ├── encoders.py
│   │   ├── evidence.py
│   │   ├── router.py
│   │   ├── top_p.py
│   │   ├── fine_filter.py
│   │   ├── decoder.py
│   │   └── hidfilter.py
│   └── runtime/
│       ├── accelerator.py
│       ├── trainer.py
│       ├── evaluator.py
│       ├── checkpoint.py
│       ├── diagnostics.py
│       └── profiler.py
├── scripts/
│   ├── run_stid_pems08.sh
│   ├── build_candidates_pems08.sh
│   └── run_hidfilter_pems08.sh
└── tests/
    ├── protocol/
    ├── candidates/
    ├── model/
    ├── runtime/
    └── npu/
```

目录可按 BasicTS 实际接入方式调整，但职责边界不变。

---

# 69. Code Quality

必须：

1. 关键函数 docstring 标 tensor shape；
2. 统一 dimension order；
3. 不写死 dataset N；
4. 无 magic number；
5. forward 不读 disk；
6. model 不访问 runner state；
7. candidate offline；
8. NPU logic 不进入 model；
9. test-first 处理数值 primitive；
10. 每个 Phase 后先 profile 再继续；
11. 不允许为赶进度把 correctness check 全删掉；
12. 也不允许为“安全”在 production 每 primitive `.item()`。

---

# 70. Codex 每个 Phase 的交付格式

每个 Phase 完成后必须给出：

```text
1. 实现内容
2. 修改文件
3. 新增测试
4. 测试结果
5. PEMS08 tensor shapes
6. forward/backward timing
7. NPU peak memory
8. profiler top ops
9. 与上一 Phase 的性能变化
10. 已知问题
11. 是否建议进入下一 Phase
```

没有 performance report，不进入下一 Phase。

---

# 71. 最终 PEMS08 完整 Gate

完整 HiDFilter 至少通过：

```text
G1  BasicTS protocol parity
G2  STID sanity baseline normal
G3  deterministic candidates
G4  lag mapping
G5  safe softmax
G6  deterministic Top-p
G7  CPU FP32 full step
G8  NPU FP32 full step
G9  fixed batch overfit
G10 family availability/empty path
G11 multi-lag selection
G12 family warm-up crossed
G13 best checkpoint reload
G14 resume
G15 diagnostics on/off prediction unchanged
G16 no development test access
G17 profile shows no obvious host-sync pathology
G18 production path no forbidden large tensors
```

以上未全部通过，不扩六数据集。

---

# 72. 正式多数据集顺序

完整 PEMS08 稳定后：

```text
PEMS04
PEMS07
PEMS03
METR-LA
PEMS-BAY
```

PEMS07 是主要 scalability check。

每新增 dataset 必须先：

```text
inspect graph artifact
freeze graph semantics
build candidate
small smoke
full run
```

禁止直接把其他 dataset config 复制过去就训练。

---

# 73. 实验失败处理规则

如果效果未达预期，按最小变量原则诊断。

禁止第一反应：

```text
加 Transformer
加 GCN
加更多 loss
加 attention head
调几十个 hyperparameters
```

先确定：

```text
protocol
data
candidate
mapping
optimization
support
runtime
```

都没有问题。

---

# 74. 第一阶段成功定义

第一阶段不是“必须马上超过所有 baseline”。

第一阶段成功是：

```text
1. BasicTS + STID runtime 正常
2. HiDFilter 实现符合数学定义
3. PEMS08 能稳定训练
4. loss 正常下降
5. validation metric 合理
6. NPU 速度没有明显工程浪费
7. seed 重复稳定
8. 关键结构对照实验给出方向性信号
```

完成这些后，才进入更大规模实验。

---

# 75. Codex 第一轮固定回复模板

Codex 完整读取本文后只能回复：

```text
HiDFilter 实现理解报告

1. BasicTS 实验脚手架
- 计划复用：
- Ascend 需要适配：
- 明确不重新实现：

2. STID Phase 0
- 使用方式：
- PEMS08 配置：
- 运行命令：
- timing：
- metric：
- pass gate：

3. HiDFilter 模块树
<tree>

4. Tensor Flow
- history：
- context：
- lag_content：
- family evidence：
- router：
- lag_token：
- global K/V：
- gather：
- Fine probability：
- Edge Top-p：
- family message：
- fusion：
- decoder：

5. 性能关键设计
- no [B,N,H,C,D]：
- no [B,N,K,L,D]：
- global projection before gather：
- static metadata：
- host sync：
- diagnostics：

6. Phase 1 Self-only
- tasks：
- tests：
- timing：
- pass gate：

7. 后续 Phase
- Phase 2：
- Phase 3：
- Phase 4：
- Phase 5：
- Phase 6：
- Phase 7：
- Phase 8：

8. 当前硬件与并行实验计划
- 已识别 NPU：
- 单 run device policy：
- 8-card independent-run scheduling：
- CPU/DataLoader concurrency policy：
- NPU monitoring：
- 是否需要 DDP：默认否，并说明原因

9. 当前缺失外部信息
- adjacency semantics：
- dataset paths：
- BasicTS version/source：
- 当前 torch / torch_npu / CANN 版本：
- host CPU / RAM / NUMA topology：
- 其他：

STOP。
等待批准，不修改工作区。
```

---

# 76. 最终执行原则

整个项目按照下面的因果顺序推进：

```text
BasicTS protocol
        ↓
STID sanity
        ↓
Self-only HiDFilter
        ↓
Physical
        ↓
Semantic
        ↓
Fine multi-lag
        ↓
Family Evidence Router
        ↓
Family Top-p
        ↓
Diagnostics
        ↓
PEMS08 full experiment
        ↓
关键结构对照实验
        ↓
六数据集扩展
```

任何一步失败，都先修当前一步。

不要通过增加下一层复杂度掩盖上一层错误。

HiDFilter 最终实现的核心始终是：

\[
\boxed{
A^r_{i,h,j,\tau}
=
\tilde r_{i,h,r}
\cdot
\tilde a^r_{i,h,j,\tau}
}
\]

其中：

```text
tilde r
= dependency-family filtering

tilde a
= sensor-lag filtering
```

模型、训练、性能和实验流程都围绕这两个层级展开。
