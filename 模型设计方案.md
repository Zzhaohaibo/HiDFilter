# HiDFilter 模型设计方案

> **项目：** HiDFilter — Hierarchical Dependency Filtration for Traffic Forecasting  
> **任务：** 多步交通预测，历史 12 步 → 未来 12 步  
> **文档角色：** 本文件是 **HiDFilter 模型本体的唯一权威设计规格**。它只定义模型“是什么”，不定义当前 GPU、训练脚本、BasicTS、Git、pytest、部署方式或实施 Phase。  
> **核心原则：** HiDFilter 只有一个中心机制：**Hierarchical Dependency Filtration**。Self / Physical / Semantic 是三类候选依赖空间，不是三套预测专家。

---

# 1. 研究问题与核心假设

对于目标传感器 \(i\) 和预测 horizon \(h\)，交通预测真正需要回答的不只是“依赖哪个传感器”，而是两个层级的问题：

1. 当前样本、目标节点、预测 horizon 应该信任哪一类依赖空间？
2. 在被考虑的依赖空间内，哪些具体的 \((source\ sensor\ j, historical\ lag\ \tau)\) 实例应该被保留？

HiDFilter 用以下层次分解表达这一点：

\[
\boxed{
P(r,j,\tau\mid X,i,h)
=
P(r\mid c_i,u_i^r,e_i,e_h)
\cdot
P(j,\tau\mid r,c_i,z_{j,\tau},e_i,e_h,\pi_{ij}^r)
}
\]

其中：

- \(X\)：历史交通序列；
- \(i\)：target sensor；
- \(h\in\{1,\dots,12\}\)：forecast horizon；
- \(r\in\{\text{Self},\text{Physical},\text{Semantic}\}\)：dependency family；
- \(j\)：source sensor；
- \(\tau\in\{1,\dots,12\}\)：历史 lag，\(\tau=1\) 表示最新历史值，\(\tau=12\) 表示最老历史值；
- \(c_i\)：目标节点当前交通上下文；
- \(u_i^r\)：family-level、identity-free 的粗粒度证据；
- \(e_i\)：node embedding；
- \(e_h\)：horizon embedding；
- \(z_{j,\tau}\)：identity-aware sensor-lag token；
- \(\pi_{ij}^r\)：候选关系先验。

最终 joint coefficient：

\[
\boxed{
A^r_{i,h,j,\tau}
=
\tilde r_{i,h,r}
\cdot
\tilde a^r_{i,h,j,\tau}
}
\]

其中 \(\tilde r\) 是 family-level filtration 后的权重，\(\tilde a\) 是 family 内 sensor-lag filtration 后的权重。

---

# 2. 创新边界

HiDFilter 的主要研究贡献应围绕以下三点理解：

1. **Hierarchical dependency formulation**：同时建模 dependency-family 层和具体 sensor-lag instance 层；
2. **Dependency-space routing**：Router 选择的是依赖搜索空间，不是 forecasting expert；
3. **Fine multi-lag filtration**：Fine stage 直接过滤 \((j,\tau)\)，允许同一个 source 同时保留多个 lag，并通过 Top-p 得到 variable-cardinality support。

以下内容不是独立创新 claim：

- physical graph；
- semantic graph；
- Pearson correlation；
- shortest path；
- Top-p 本身；
- traffic delay 本身；
- candidate-first 本身；
- MLP context encoder。

Candidate-first 的作用是让细粒度 sensor-lag filtration 在大图上可计算，而不是作为论文主创新。

---

# 3. 模型总体结构

```text
Historical Traffic X [B,12,N,1]
        │
        ├───────────────────────────────┐
        │                               │
        ▼                               ▼
Target Context Encoder            Lag Content Encoder
        │                               │
        ▼                               ▼
context c [B,N,D]          lag_content ℓ [B,N,12,D]
        │                               │
        │                    ┌──────────┴──────────┐
        │                    │                     │
        │                    ▼                     ▼
        │              Family Evidence       + source identity
        │                    │                     │
        │                    ▼                     ▼
        │               Shared Router       Fine Lag Token z
        │                    │                     │
        │              Family Top-p                │
        │                    │                     │
        └────────────────────┼──────────────┐      │
                             │              │      │
                             │              ▼      ▼
                             │       Shared Fine Sensor-Lag Filter
                             │       family-wise softmax + Edge Top-p
                             │              │
                             └───────┬──────┘
                                     ▼
                           Hierarchical Fusion
                                     │
                                     ▼
                                Decoder
                                     │
                                     ▼
                         prediction [B,12,N,1]
```

三类 family 只是候选依赖空间。全模型只有：

- 一个 Target Context Encoder；
- 一个 Lag Content Encoder；
- 一个共享 Router scorer；
- 一个共享 \(W_q\)；
- 一个共享 \(W_k\)；
- 一个共享 \(W_v\)；
- 一个共享 decoder；
- 一个全局共享先验强度标量 \(\alpha\)。

禁止把 Self / Physical / Semantic 写成三套预测网络、三套 decoder、三套 Q/K/V 或三个独立 expert。

---

# 4. 固定维度与索引约定

| 符号 | 含义 | 默认值 |
|---|---|---:|
| \(L\) | 历史长度 | 12 |
| \(H\) | 预测长度 | 12 |
| \(R\) | family 数 | 3 |
| \(D\) | hidden width | 64 |
| \(D_n\) | node embedding width | 16 |
| \(D_l\) | lag embedding width | 16 |
| \(D_h\) | horizon embedding width | 16 |
| \(D_f\) | family embedding width | 16 |
| \(K_p\) | Physical source pool | 8 |
| \(K_s\) | Semantic source pool | 8 |
| \(\rho_f\) | Family Top-p threshold | 0.8 |
| \(\rho_e\) | Edge Top-p threshold | 0.8 |
| dropout | dropout | 0.1 |

Family ID 固定：

```text
0 = Self
1 = Physical
2 = Semantic
```

## 4.1 Lag 约定

论文、日志、diagnostics 统一：

\[
\tau\in\{1,\dots,12\}
\]

含义：

```text
tau=1  -> latest historical observation
tau=12 -> oldest historical observation
```

代码内部：

\[
lag\_index=\tau-1
\]

输入历史按 oldest → newest：

```text
history [B,12,N,1]
```

因此：

\[
x^{lag}_{j,lag\_index}
=
history[:,11-lag\_index,j,0]
\]

任何公开 API、diagnostic 或论文表述禁止把 latest 称为 lag 0。

## 4.2 Candidate flatten 约定

固定 sensor-major / lag-minor：

\[
\boxed{edge\_index=sensor\_rank\times12+lag\_index}
\]

同一 source 的 12 个 lag 连续排列。该顺序也是 Edge Top-p 的 canonical tie-breaking 顺序。

---

# 5. 输入与模型信息边界

主模型输入：

```text
history [B,12,N,1]
```

输出：

```text
prediction [B,12,N,1]
```

模型主路径只读取 traffic channel。外部 TOD、DOW、timestamps、天气、节假日等不进入主模型。

允许的内部 learnable embedding：

- node embedding \(e_i^{node}\)；
- lag embedding \(e_\tau^{lag}\)；
- horizon embedding \(e_h^{hor}\)；
- route-family embedding \(e_r^{route}\)；
- fine-family embedding \(e_r^{fine}\)。

route-family embedding 与 fine-family embedding 必须是两套独立参数，因为它们服务于不同语义层级。

---

# 6. Candidate Dependency Spaces

每个 target sensor \(i\) 有三个候选空间：Self、Physical、Semantic。

候选 pool 大小固定，不做 variable-cardinality candidate construction；variable cardinality 只由后续 Top-p 决定。

```text
Self:      1 source × 12 lags = 12 positions
Physical:  Kp=8 sources × 12 lags = 96 positions
Semantic:  Ks=8 sources × 12 lags = 96 positions
```

最大 candidate positions：

\[
C_{max}=12+96+96=204
\]

候选构造必须离线完成。模型 forward 不允许在线构图、计算 Pearson、跑 shortest path 或决定 candidate source 集合。

## 6.1 Candidate metadata

每个 family 至少需要静态 metadata：

```text
source_index      [N,C_r] int64
lag_index         [N,C_r] int64
flat_index        [N,C_r] int64
valid             [N,C_r] bool
prior             [N,C_r] float32
```

其中：

\[
flat\_index=source\_index\times12+lag\_index
\]

padding 位置：

```text
source_index = 0
valid = false
prior = 0
```

padding 不得参与任何 softmax、Top-p 或 message。

---

# 7. Self Candidate

对 target \(i\)：

```text
source = i
lags   = tau=1..12
valid  = true
prior  = 1
```

因此 Self 始终 available。

Self 的 12 个 candidate positions 按 lag_index 0..11 排列。

---

# 8. Physical Candidate

Physical candidate 的职责是提供简单、确定、可复现的 topology-constrained dependency search space，而不是成为独立复杂算法。

## 8.1 Graph contract

每个数据集必须在候选构造阶段显式提供：

```text
graph_mode: directed | undirected
weight_semantics: affinity | distance | cost
conversion_scale: null | positive scalar
```

禁止根据 adjacency 数值自动猜测语义。

定义正边：

```text
A[src,dst] > 0
```

self-loop 不用于 candidate ranking。

### affinity

对所有正边以数据集级最大正值归一化：

\[
w_{uv}=A_{uv}/\max(A_{positive})
\]

### distance / cost

必须显式给出 \(conversion\_scale>0\)：

\[
w_{uv}
=
\exp\left[-\left(\frac{A_{uv}}{conversion\_scale}\right)^2\right]
\]

随后对正权重按最大值归一化。

## 8.2 Reachability

对于 target \(i\)：

- directed graph：合法 source \(j\neq i\) 必须存在路径 \(j\rightarrow\cdots\rightarrow i\)；
- undirected graph：合法 source \(j\neq i\) 必须与 \(i\) 连通。

**不存在 disconnected fallback。** 不可达节点不能为了凑满 \(K_p\) 被补入。

## 8.3 Ranking

对每个合法 source \(j\)，计算：

1. shortest hop distance \(d(j,i)\)；
2. 在所有 shortest-hop path 中的最大 path strength：

\[
S(j,i)
=
\max_{p\in\mathcal P_{shortest}(j,i)}
\prod_{(u,v)\in p} w_{uv}
\]

为了数值稳定，path product 的比较应等价地在 log-space 计算。

排序唯一规则：

```text
(shortest_hop ascending,
 path_strength descending,
 sensor_index ascending)
```

取前 \(K_p=8\)。不足 8 个时 padding，不 refill。

Physical prior：

\[
\pi^{phy}_{ij}=S(j,i)
\]

其范围应在 \((0,1]\) 内。

---

# 9. Semantic Candidate

Semantic candidate 的职责是提供 topology-complementary statistical dependency search space。

它只允许使用训练集 raw traffic，以及 Physical candidate 构造所需的图结构信息。

## 9.1 First difference

对 sensor \(j\)：

\[
\Delta x_{t,j}=x_{t,j}-x_{t-1,j}
\]

只有当 raw \(x_{t,j}\) 与 \(x_{t-1,j}\) 都有效时，该 difference 才有效。

## 9.2 Pair-specific Pearson

每对 sensors 使用两者 first-difference 的 common-valid samples 计算 Pearson correlation。

规则：

```text
min_overlap = 288
variance <= 1e-12 -> pair unavailable
correlation clipped to [-1,1]
ranking uses abs(correlation)
```

候选统计应使用足够精度的 CPU 计算；最终 ranking/prior 可存 float32。

## 9.3 Exclusion

对 target \(i\)，Semantic 合法 source 必须排除：

1. self \(i\)；
2. graph 中与 \(i\) 直接相连的一跳物理邻居（任意合法边方向只要表示直接拓扑连接）；
3. 最终选中的 Physical source 集合。

因此 Semantic 表达的是对 local/topological dependency 的补充，而不是重复拷贝 Physical 空间。

## 9.4 Ranking

对剩余合法 source：

```text
abs(correlation) descending
sensor_index ascending
```

取 \(K_s=8\)。不足时 padding，不 refill。

Semantic prior：

\[
\pi^{sem}_{ij}=|corr_{ij}|
\]

signed correlation 可作为离线诊断 metadata 保存，但不进入主 Fine score。

---

# 10. Family Availability

对 target \(i\)：

\[
A_i=
[
1,
\mathbf 1(any\ physical\ valid),
\mathbf 1(any\ semantic\ valid)
]
\]

即：

```text
Self      always available
Physical  available iff >=1 valid physical source
Semantic  available iff >=1 valid semantic source
```

Unavailable family 必须满足：

```text
router probability = 0
family Top-p keep = false
family weight = 0
family message contribution = 0
```

---

# 11. Target Context Encoder

Target Context 只负责描述目标 sensor 最近 12 步的 traffic state，不承担复杂 temporal backbone 角色。

输入：

```text
history [B,12,N,1]
```

对每个 sensor 独立重排为：

```text
target_history [B,N,12]
```

网络：

```text
Linear(12,D)
GELU
Linear(D,D)
LayerNorm(D)
```

数学形式：

\[
\boxed{
c_i
=
LN\left(W_2\,GELU(W_1x_i+b_1)+b_2\right)
}
\]

输出：

```text
context c [B,N,D]
```

允许信息：

- target sensor 自己的 12 步 traffic history。

禁止：

- spatial aggregation；
- candidate information；
- graph information；
- family identity；
- timestamps/TOD/DOW；
- future traffic。

---

# 12. Lag Content Encoder

Lag Content 必须保持 **identity-free、pointwise、exact-lag**。

先把历史转换为 lag 顺序：

```text
lag_values [B,N,12,1]
```

其中 index 0 对应 \(\tau=1\) latest。

对每个 \((j,\tau)\)：

\[
\boxed{
\ell_{j,\tau}
=
MLP_{lag}(x_{j,\tau})
+
P_l(e_\tau^{lag})
}
\]

其中：

```text
MLP_lag:
Linear(1,D)
GELU
Linear(D,D)

lag_embedding: [12,D_l]
P_l: Linear(D_l,D)
```

输出：

```text
lag_content ℓ [B,N,12,D]
```

允许信息：

- 当前 exact lag 的 traffic value；
- lag identity。

严格禁止：

- source node identity；
- target node identity；
- family identity；
- candidate rank/index；
- candidate prior；
- Fine score；
- timestamps；
- temporal convolution/attention/pooling；
- 其它 lag 的 traffic content。

因此改变一个 \((sensor,time)\) 的输入，只应直接改变对应 sensor-lag 的 content token，不应通过 Lag Content Encoder 扩散到其它 lag。

---

# 13. Node / Horizon / Family Embeddings

Learnable parameters：

```text
node_embedding        [N,D_n]
lag_embedding         [12,D_l]
horizon_embedding     [12,D_h]
route_family_embedding[3,D_f]
fine_family_embedding [3,D_f]
```

Projection：

```text
P_n: Linear(D_n,D)
P_h: Linear(D_h,D)
P_route/fine 按各自模块需要投影
```

route-family 与 fine-family embedding 不共享。

---

# 14. Fine Lag Token

Fine path 才允许加入 source identity。

\[
\boxed{
z_{j,\tau}
=
LN\left(\ell_{j,\tau}+P_n(e_j^{node})\right)
}
\]

输出：

```text
lag_token z [B,N,12,D]
```

语义：`z[b,j,tau]` 表示 source sensor \(j\) 在精确 lag \(\tau\) 上的 identity-aware token。

它只用于 Fine Sensor-Lag Filter，不用于 Family Evidence。

---

# 15. Family Evidence

Router 需要 coarse evidence，但不能提前看到 Fine-level identity。

对 family \(r\) 和 target \(i\)：

\[
\boxed{
u_i^r
=
Mean_{(j,\tau)\in C_i^r}\ell_{j,\tau}
}
\]

因为每个合法 source 的 12 个 lag 都完整进入候选空间，该式可高效等价实现为：

1. 对每个 source 先做 FP32 lag mean：

\[
\bar\ell_j=Mean_{\tau}^{FP32}(\ell_{j,\tau})
\]

得到：

```text
lag_mean [B,N,D]
```

2. 按 family source candidate gather；
3. 对 valid source 做无权 masked mean。

Self evidence 就是 target 自身的 lag mean。

输出：

```text
family_evidence u [B,N,3,D]
```

Family Evidence 必须满足：

```text
parameter-free
unweighted
FP32 reduction
candidate-order invariant
empty family -> exact zero
```

严格禁止：

- source node identity；
- candidate prior；
- candidate rank；
- Fine key/value；
- Fine score/probability；
- Edge Top-p support；
- attention pooling。

这是 coarse-to-fine 信息边界的核心。

---

# 16. Shared Family Router

Router 回答：

> 对当前样本、target sensor 和 forecast horizon，哪类 dependency space 应被激活？

对每个 \((i,h,r)\)，构造：

\[
\xi_{i,h,r}
=
[
 c_i
\Vert e_i^{node}
\Vert e_h^{hor}
\Vert u_i^r
\Vert e_r^{route}
]
\]

维度：

\[
2D+D_n+D_h+D_f=176
\]

共享 scorer：

```text
Linear(176,D)
GELU
Linear(D,1)
```

同一个 scorer 对三个 family 重复使用，禁止 family-specific Router MLP。

输出：

```text
router_logits      [B,N,H,3]
router_prob_dense  [B,N,H,3]
```

Router softmax **只沿 family 维度**进行。对固定的 `(b,i,h)`，先用 `family_available[i,r]` 屏蔽 unavailable family，再在 available families 之间归一化：

\[
p^{fam}_{b,i,h,r}
=
\frac{\exp(g_{b,i,h,r})}
{\sum_{s:A_{i,s}=1}\exp(g_{b,i,h,s})}
\qquad (A_{i,r}=1)
\]

unavailable family 的概率必须是 exact 0。因为 Self 始终 available，所以每个 Router row 至少有一个合法 family，并满足：

\[
\sum_r p^{fam}_{b,i,h,r}=1.
\]

这里的 family softmax 与后文 Fine softmax 是两个不同的归一化层级：Router 在 `R=3` 个 family 上归一化；Fine stage 则在**每个 family 自己的 sensor-lag candidates 内**独立归一化。二者不得合并。

Router 允许读取：

- target context；
- target node identity；
- forecast horizon identity；
- 当前 family 的 identity-free pooled evidence；
- route-family identity。

Router 严格禁止读取：

- candidate source identity；
- candidate lag identity；
- candidate prior；
- candidate rank/index；
- Fine K/V；
- Fine logits/probability；
- Edge Top-p support；
- family message。

---

# 17. Family Top-p

在 availability-masked dense Router probability 上执行：

\[
\rho_f=0.8
\]

Top-p support 由 detached FP32 probability 决定；保留后的权重必须从原始 non-detached probability 中取值并做精确正值重归一化。

Family canonical order：

```text
Self < Physical < Semantic
```

输出：

```text
family_keep    [B,N,H,3] bool
family_weight  [B,N,H,3] float
```

有效 row 至少保留一个 available family。只有 unavailable families 的假设不可能出现，因为 Self 始终 available。

Family Top-p 只改变 support，不改变三个 family 的 Fine 数学定义。

---

# 18. Fine Query

对 target \(i\)、horizon \(h\)：

\[
\boxed{
q_{i,h}
=
W_q\,LN\left(
 c_i
 +P_n(e_i^{node})
 +P_h(e_h^{hor})
\right)
}
\]

输出：

```text
Q [B,N,H,D]
```

全模型只有一个 \(W_q\)。

---

# 19. Global Fine K/V

这是模型结构和复杂度的重要约束。

先全局构造 identity-aware token：

\[
Z=LN\left(\ell+P_n(e^{node})\right)
\]

```text
Z [B,N,12,D]
```

Value：

\[
V=W_v Z
\]

```text
V_global [B,N,12,D]
```

只计算一次。

对每个 family \(r\)：

\[
K^r
=
W_k\left(Z+P_f(e_r^{fine})\right)
\]

```text
K_global_r [B,N,12,D]
```

全模型只有一个 \(W_k\) 和一个 \(W_v\)。

family-specific 行为只来自：

- candidate space；
- fine-family embedding；
- prior。

禁止：

- family-specific Wq/Wk/Wv；
- gather 后再对每个 target 重复 LayerNorm/K/V projection。

强制数据流：

```text
lag_content
→ global identity-aware token
→ global projection
→ target-specific candidate gather
```

---

# 20. Candidate Gather

将全局 source-lag 维展平：

```text
[B,N*12,D]
```

利用静态：

```text
flat_index [N,C_r]
```

一次 gather 得到：

```text
candidate_K [B,N,C_r,D]
candidate_V [B,N,C_r,D]
```

禁止把 `[B,N*12,D]` expand 成 `[B,N,N*12,D]`。

禁止 materialize：

```text
[B,N,K,12,D]
[B,N,H,C,D]
```

Fine 计算只允许核心 tensor：

```text
Q      [B,N,H,D]
K/V    [B,N,C,D]
score  [B,N,H,C]
message[B,N,H,D]
```

---

# 21. Fine Score 与全局共享 Prior Strength

对 family \(r\)：

\[
\boxed{
s^r_{i,h,j,\tau}
=
\frac{q_{i,h}^{\top}k^r_{j,\tau}}{\sqrt D}
+
\alpha\log(\max(\pi^r_{ij},10^{-6}))
}
\]

其中：

\[
\boxed{\alpha=Softplus(a)}
\]

**全模型只有一个全局共享 \(a\) / \(\alpha\)**，Self、Physical、Semantic 共用。

初始化：

\[
\alpha=1
\]

即 \(a=Softplus^{-1}(1)\)。

禁止：

```text
alpha_self
alpha_phy
alpha_sem
family-specific alpha_raw
```

Self prior 恒为 1，因此其 prior bias 为 0。

Physical / Semantic prior 只进入 Fine score，不允许进入 Router 或 Family Evidence。

---

# 22. Safe FP32 Masked Softmax

Fine stage 对每个 family 独立做 candidate softmax。对固定 `(b,i,h,r)`，只在该 family 的 candidate 维度 `c=(j,τ)` 上归一化：

\[
a^{r,dense}_{b,i,h,c}
=
Softmax_{c\in C_i^r}(s^r_{b,i,h,c}).
\]

因此：

```text
Self     : softmax over C_self = 12
Physical : softmax over C_phy  = 96（只对 valid candidates）
Semantic : softmax over C_sem  = 96（只对 valid candidates）
```

这与 Router 的 family softmax 不同。Router 在 family 维度 `r` 上归一化；Fine 在固定 family 内的 sensor-lag candidate 维度 `c` 上归一化。

必须满足：

```text
computation in FP32
invalid probability = exact 0
valid non-empty row sum = 1
all-invalid row = all zeros
```

不能把三个 family 的 candidate union 后统一 softmax。

合法位置存在 NaN/Inf 属于模型错误，不能 `nan_to_num` 或静默修复。

这里定义的是数学合同；具体如何在 production 中高效触发 hard failure 属于执行/工程层，不在本文件规定同步方式。

---

# 23. Deterministic Top-p Operator

Family Top-p 与 Edge Top-p 使用同一数学定义。

合法：

\[
0<\rho\le1
\]

对 detached FP32 probability 降序排列：

\[
K_\rho
=
\min\left\{k:\sum_{l=1}^{k}p_{(l)}\ge\rho\right\}
\]

crossing item 必须保留。

FP32 crossing tolerance：

\[
\delta_n=4(n+1)\epsilon_{32}
\]

使用：

\[
C_k+\delta_n\ge\rho
\]

若 \(\rho=1\)，保留所有 valid positions。

## 23.1 Tie rule

support 的确定：

1. detached FP32 probability 决定 support size 和 cutoff；
2. `p > cutoff` 全保留；
3. `p == cutoff` 按 canonical order 填满剩余 quota。

Family canonical order：

```text
Self, Physical, Semantic
```

Edge canonical order：

\[
sensor\_rank\times12+lag\_index
\]

support 必须 detached；最终 weight 使用原始 non-detached probability 并对正权重精确重归一化。

禁止 denominator `+eps` 造成概率和漂移。

---

# 24. Edge Top-p

每个 family、每个 \((b,i,h)\) 独立执行：

\[
\rho_e=0.8
\]

输出：

```text
edge_keep_r   [B,N,H,C_r]
edge_weight_r [B,N,H,C_r]
```

Edge Top-p 的最小单位是 `(source sensor, exact lag)`。

因此允许同一个 source 同时保留：

```text
(j,tau=1)
(j,tau=4)
(j,tau=7)
```

禁止先按 source 聚合再只保留一个 lag；禁止主模型对每个 source 做 argmax-lag。

---

# 25. Family Message

对 family \(r\)：

\[
\boxed{
m^r_{i,h}
=
\sum_{j,\tau}
\tilde a^r_{i,h,j,\tau}
v_{j,\tau}
}
\]

实现推荐数学形式：

```text
scores  = einsum("bnhd,bncd->bnhc", Q, K)
message = einsum("bnhc,bncd->bnhd", edge_weight, V)
```

输出：

```text
family_message_r [B,N,H,D]
```

Unavailable family message 为 exact zero。

---

# 26. Hierarchical Fusion

最终 fused dependency message：

\[
\boxed{
m_{i,h}
=
\sum_r
\tilde r_{i,h,r}m^r_{i,h}
}
\]

输出：

```text
fused_message [B,N,H,D]
```

在 active/valid hierarchy 上：

\[
\sum_{r,j,\tau}A^r_{i,h,j,\tau}=1
\]

主路径应逐 family 累加 fused message；不要求长期保存 `[B,N,H,3,D]`。

---

# 27. Decoder

主模型固定 residual decoder，不再做 architecture selection。

Decoder 输入：

\[
[c_i\Vert m_{i,h}\Vert e_h^{hor}]
\]

维度：

\[
2D+D_h=144
\]

网络：

```text
Linear(144,D)
GELU
Dropout(0.1)
Linear(D,1)
```

预测 residual：

\[
\Delta_{i,h}=Decoder([c_i\Vert m_{i,h}\Vert e_h])
\]

最终：

\[
\boxed{
\hat y_{i,h}=x_{i,latest}+\Delta_{i,h}
}
\]

一个 decoder 负责全部 family 和 horizon。

---

# 28. Model Forward Contract

推荐纯模型 API：

```python
forward(
    history,
    *,
    apply_family_top_p: bool,
    collect_diagnostics: bool = False,
)
```

主输出：

```text
prediction [B,12,N,1]
```

`apply_family_top_p` 只控制 family-level hard support 是否应用；Edge Top-p 始终属于主模型 Fine filtration。

模型本体不得知道：

- epoch；
- optimizer；
- scaler；
- dataset path；
- candidate artifact path；
- GPU/NPU device type；
- BasicTS；
- checkpoint；
- run stage。

Candidate metadata 应在构造模型时注入并注册为非持久 buffer 或等价静态状态。

---

# 29. Training Objective Boundary

HiDFilter 主模型只要求 prediction objective。

不定义 Router balance loss，不强迫三个 family 平均使用。

正式训练目标应是 masked prediction MAE；具体 normalization/mask/optimizer 等属于实验执行协议，不属于本模型结构文档。

Router collapse 是否发生，应首先通过 diagnostics 和实验判断，而不是预先加入手工 balance regularizer。

---

# 30. 复杂度与内存边界

每个 target 最大 candidate positions：

\[
C=204
\]

Fine scoring 主复杂度：

\[
O(BNHCD)
\]

而不是全图 sensor-lag 枚举：

\[
O(BHN^2LD)
\]

Candidate-first 只限制搜索空间；不要声称 Family Router 自动跳过所有 Fine FLOPs，除非未来真正实现动态 sparse kernel。

主模型禁止 materialize：

```text
[B,N,K,L,D]
[B,N,H,C,D]
[B,N,N*L,D]
```

主 Fine path 应保持：

```text
Q       [B,N,H,D]
K/V     [B,N,C,D]
scores  [B,N,H,C]
message [B,N,H,D]
```

---

# 31. Diagnostics 语义

Diagnostics 不是第二条模型路径，开启或关闭 diagnostics 必须不改变 prediction。

下面所有统计量都使用**唯一固定定义**。论文、表格、可视化和代码中的同名指标不得另行解释。

## 31.1 记号与最终有效依赖

令一个 evaluation query 为：

\[
q=(b,i,h).
\]

正式 validation/test 聚合只统计 evaluator 判定为有效 target 的 query，记集合为 \(\mathcal Q_v\)，大小为 \(Q_v\)。模型 forward 本身不读取 target validity；有效 query mask 属于 evaluator/diagnostics aggregator。

对 family \(r\)：

- \(A_{q,r}\in\{0,1\}\)：family availability；
- \(p^{fam}_{q,r}\)：Family Top-p **之前**的 dense Router probability；
- \(K^{fam}_{q,r}\in\{0,1\}\)：Family Top-p support；
- \(\tilde r_{q,r}\)：Family Top-p 后精确重归一化的 sparse family weight；
- \(a^{r,dense}_{q,c}\)：Edge Top-p **之前**、family 内独立 softmax 得到的 dense Fine probability；
- \(K^{edge,r}_{q,c}\in\{0,1\}\)：Edge Top-p support；
- \(V^r_{i,c}\in\{0,1\}\)：candidate validity。

最终有效 edge 定义为：

\[
\boxed{
K^{eff,r}_{q,c}
=
V^r_{i,c}
\land
K^{edge,r}_{q,c}
\land
K^{fam}_{q,r}
}
\]

只有 `effective_edge_keep = candidate_valid AND edge_keep AND family_keep` 可以用于关于最终 selected dependency 的论文统计。单独的 `edge_keep` 不能冒充最终依赖。

## 31.2 Family diagnostics

### Family availability rate

对 family \(r\)：

\[
Availability_r
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}A_{q,r}.
\]

Self 理论上恒为 1。

### Family activation rate conditioned on availability

\[
Activation_r
=
\frac{
\sum_{q\in\mathcal Q_v}A_{q,r}K^{fam}_{q,r}
}{
\sum_{q\in\mathcal Q_v}A_{q,r}
}.
\]

若 denominator 为 0，则该 family 的 activation 记为 unavailable / undefined，而不是伪造 0。

### Dense family usage

\[
DenseUsage_r
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}p^{fam}_{q,r}.
\]

由于 unavailable family 的 dense probability 为 exact 0：

\[
\sum_r DenseUsage_r=1.
\]

### Sparse family usage

\[
SparseUsage_r
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}\tilde r_{q,r}.
\]

并满足：

\[
\sum_r SparseUsage_r=1.
\]

代码和分析中的 `Physical usage` / `Semantic usage` 默认指对应 family 的 `SparseUsage_r`；如果报告 dense usage，必须明确写 `dense`。

### Active-family count

每个 query：

\[
C^{fam}_q=\sum_r K^{fam}_{q,r}.
\]

全 split：

\[
ActiveFamilyCount
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}C^{fam}_q.
\]

### Dense Router entropy

对 dense Router probability：

\[
H_q
=
-\sum_{r:A_{q,r}=1}
p^{fam}_{q,r}\log p^{fam}_{q,r},
\]

其中 `0 log 0 = 0`。聚合：

\[
RouterEntropy
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}H_q.
\]

默认报告 natural-log entropy，不做除以 `log(number_of_available_families)` 的归一化；若未来需要 normalized entropy，必须另起指标名。

### Family retained mass

Family Top-p 的 retained mass 必须使用**重归一化之前的 dense Router probability**：

\[
M^{fam}_q
=
\sum_r p^{fam}_{q,r}K^{fam}_{q,r}.
\]

聚合：

\[
FamilyRetainedMass
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}M^{fam}_q.
\]

禁止用 Top-p 后的 \(\tilde r\) 求和作为 retained mass，因为其和按定义恒为 1，没有分析意义。

若需要 per-family retained contribution：

\[
FamilyRetainedContribution_r
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}p^{fam}_{q,r}K^{fam}_{q,r}.
\]

## 31.3 Edge diagnostics

### Edge retained mass

对 family \(r\)，只在该 family available 的 query 上统计 Edge Top-p 本身保留的 dense Fine mass：

\[
M^{edge,r}_q
=
\sum_c a^{r,dense}_{q,c}K^{edge,r}_{q,c}.
\]

令：

\[
\mathcal Q_r=\{q\in\mathcal Q_v:A_{q,r}=1\}.
\]

则：

\[
EdgeRetainedMass_r
=
\frac{1}{|\mathcal Q_r|}
\sum_{q\in\mathcal Q_r}M^{edge,r}_q.
\]

若 \(|\mathcal Q_r|=0\)，该指标为 unavailable / undefined。

这里故意不乘 `family_keep`：它测量的是 **Edge Top-p operator 在该 family 内保留了多少 dense probability mass**。最终依赖数量、lag histogram 和 multi-lag 必须使用 `effective_edge_keep`。

### Effective edge count

每个 query、family：

\[
C^{edge,r}_q
=
\sum_c K^{eff,r}_{q,c}.
\]

可报告 per-family：

\[
EffectiveEdgeCount_r
=
\frac{1}{Q_v}
\sum_{q\in\mathcal Q_v}C^{edge,r}_q,
\]

以及总计：

\[
EffectiveEdgeCount
=
\sum_r EffectiveEdgeCount_r.
\]

### Lag histogram

对 \(\tau=1,\ldots,12\)，统计所有 effective edges 中 lag 为 \(\tau\) 的数量：

\[
Hist_\tau
=
\sum_{q,r,c}
K^{eff,r}_{q,c}\,
\mathbf1[lag(c)=\tau].
\]

默认同时保存 raw count 和归一化比例：

\[
LagShare_\tau
=
\frac{Hist_\tau}{\sum_{s=1}^{12}Hist_s}.
\]

若没有任何 effective edge，则 share 为 unavailable / undefined，不用 epsilon 伪造分布。

### Multi-lag ratio

把同一 `(batch sample, target, horizon, source sensor)` 的 effective selected lags 归为一组，group key **不包含 family**。这保持 multi-lag 指标只回答一个问题：最终层次过滤后，同一个 source sensor 是否同时贡献了多个历史 lag。

若某个 source-lag 在任一 family 中成为 effective edge，则该 `(source,lag)` 视为最终被选。对每组 \(g=(b,i,h,j)\)，令：

\[
n_g
=
\#\left\{
\tau:
\bigvee_r K^{eff,r}_{b,i,h,(j,\tau)}=1
\right\}.
\]

denominator：

\[
D_{multi}=\#\{g:n_g\ge1\}.
\]

numerator：

\[
N_{multi}=\#\{g:n_g\ge2\}.
\]

\[
MultiLagRatio
=
\frac{N_{multi}}{D_{multi}}.
\]

若 \(D_{multi}=0\)，该 ratio 为 unavailable / undefined。

## 31.4 Horizon-conditioned diagnostics

所有 family / edge 指标都允许按 horizon \(h\) 分组重复计算；定义完全不变，只把 \(\mathcal Q_v\) 限制为对应 horizon 的有效 query。不得为 per-horizon 版本重新定义统计口径。

## 31.5 聚合与信息边界

Diagnostics 必须按完整 validation/test split 的**总和与总计数**聚合，不能简单平均 per-batch ratio / per-batch mean。

大规模 `router_prob`、`edge_prob`、`edge_keep`、`effective_edge_keep` 不属于需要永久保存的输出。正常实现应先做 reduce，只保留上述统计所需的 compact sums / counts / 12-bin histogram / small family vectors。

这些 diagnostics 只用于解释模型行为，不宣称因果关系。

# 32. 模型不变量

Codex 实现必须保持以下不变量：

1. 三个 family 是 dependency spaces，不是 experts；
2. Router coarse，Fine stage fine-grained；
3. Family Evidence identity-free；
4. Fine token identity-aware；
5. one shared Router scorer；
6. one shared Wq/Wk/Wv；
7. one global shared alpha；
8. prior 只进入 Fine score；
9. Router softmax 只在 available family 维度上归一化；Fine softmax 则在每个 family 自己的 valid sensor-lag candidates 内独立归一化；二者不得合并；
10. Family Top-p 与 Edge Top-p 是两个不同层级；
11. support detached，权重保留梯度；
12. 同一个 source 可同时保留多个 lag；
13. unavailable family exact zero contribution；
14. residual decoder 唯一；
15. prediction 不读取 timestamp/external covariate；
16. candidate graph/correlation 不在 forward 中构造；
17. Context 不做 spatial aggregation；
18. Lag Content 不做跨-lag mixing；
19. diagnostics on/off 不改变 prediction；
20. production 主路径不创建禁止的大 tensor。

---

# 33. 明确禁止的架构扩展

未经新的权威设计裁决，禁止加入：

- Transformer backbone；
- Multi-head Attention；
- Mamba；
- GCN stack；
- Graph Transformer；
- dense learned adjacency；
- dynamic graph learner；
- forecasting MoE experts；
- 多 decoder / 多 prediction head；
- FFT / wavelet delay module；
- Granger module；
- ODE / DDE；
- contrastive loss；
- diffusion；
- adversarial training；
- Router balance regularizer；
- family-specific alpha；
- family-specific Q/K/V。

如果实验最终否定某个核心假设，应依据证据简化或重新设计，而不是通过堆更多模块掩盖问题。

---

# 34. 最终模型一句话定义

HiDFilter 是一个轻量的、candidate-first 的层次依赖过滤模型：它先根据 target state、family-level coarse evidence 和 forecast horizon 动态过滤 Self / Physical / Semantic 依赖空间，再在每个依赖空间内以 `(source sensor, exact historical lag)` 为最小单位进行 variable-cardinality Top-p filtration，最后通过层次联合权重融合依赖消息并做 residual multi-horizon forecasting。

其核心始终是：

\[
\boxed{
A^r_{i,h,j,\tau}
=
\tilde r_{i,h,r}
\tilde a^r_{i,h,j,\tau}
}
\]
